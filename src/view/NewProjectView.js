import { component, componentVoid, elementOpen, elementClose, elementVoid, text } from "wabi"

const NewProjectView = component
({
	render() {
		elementOpen("layout")

		elementClose("layout")
	}
})

export default NewProjectView